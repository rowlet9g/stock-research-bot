# FORGETMENOT 저장소 작업 지침

이 문서는 이 저장소에서 코드를 수정하는 사람과 에이전트가 따라야 하는 지속적인
개발 규칙을 정의한다.

## 프로젝트 방향

- 이 프로젝트는 개인용 주식 리서치, 투자일지, 포트폴리오 분석, 알림 도구다.
- 자동 주문은 구현하지 않는다.
- 현재 주 구현 언어는 Go다.
- Python/Streamlit 코드는 Go 포팅 검증을 위한 레거시 기준 구현이다.
- 별도 요청이 없으면 Python 쪽에 신규 기능을 추가하지 않는다.
- 작업 순서와 범위는 `docs/PROJECT_PLAN.md`를 기준으로 한다.

## 핵심 설계 규칙

- 외부 공급자별 코드는 독립된 adapter로 분리한다.
- 도메인 모델이 Yahoo, DART 등 특정 공급자의 응답 구조에 종속되지 않게 한다.
- 계산 가능한 값은 Go 코드가 계산한다.
- AI는 계산기나 데이터베이스로 사용하지 않는다.
- AI에 전달하는 모든 사실에는 가능한 범위에서 출처와 기준시각을 포함한다.
- 사실, 해석, 반론, 확인 질문을 구조적으로 분리한다.
- 예측은 단일 가격이 아니라 시나리오, 확률, 근거, 무효화 조건으로 표현한다.
- 외부 요청은 context, timeout, 재시도 한도, 오류 분류를 가져야 한다.
- 외부 API의 부분 실패가 전체 분석 실행을 불필요하게 중단시키지 않게 한다.
- 중요 분석은 하나의 비공식 데이터 공급자만으로 확정하지 않는다.

## 데이터 및 보안 규칙

- `.env`, API 키, 봇 토큰, 계좌번호, 개인정보를 커밋하지 않는다.
- 계좌 원본 CSV와 실제 포트폴리오 데이터는 기본적으로 Git에서 제외한다.
- 증권사 로그인 화면, 인증서, 비밀번호를 자동화하거나 스크래핑하지 않는다.
- 로그와 알림에 민감정보를 그대로 출력하지 않는다.
- 외부 데이터에는 provider, source URL, observed_at, fetched_at을 기록한다.
- 데이터가 없거나 오래되었으면 정상값처럼 대체하지 않는다.
- CSV import는 중복 실행해도 결과가 중복되지 않게 설계한다.

## Go 코드 규칙

- 패키지는 책임이 명확한 작은 단위로 유지한다.
- 인터페이스는 실제로 둘 이상의 구현 또는 테스트 대역이 필요할 때 도입한다.
- `internal/` 안의 도메인 로직은 CLI, HTTP, 알림 UI에 의존하지 않게 한다.
- `cmd/`는 입출력과 조립만 담당하고 계산 로직을 넣지 않는다.
- 오류에는 작업 대상과 공급자 문맥을 포함한다.
- 사용자에게 보여줄 오류와 내부 진단 오류를 구분한다.
- 시간은 저장 시 UTC를 사용하고 표시 시 시장 또는 사용자 시간대로 변환한다.
- 금액과 수량의 정밀도가 중요한 영역에서 무조건 `float64`를 사용하지 않는다.

## 테스트 규칙

- 외부 API 단위 테스트는 실제 네트워크를 호출하지 않는다.
- Yahoo, DART, KRX, SEC, FRED 응답은 fixture로 보존해 파싱을 검증한다.
- 버그 수정에는 먼저 실패를 재현하는 테스트를 추가한다.
- 시간에 의존하는 로직은 clock을 주입하거나 기준시각을 인자로 받는다.
- 분석 규칙 테스트는 경계값, 결측값, 부분 데이터를 포함한다.
- 통합 테스트와 실제 API smoke test를 구분한다.

## 기본 검증 명령

Go 파일을 수정한 뒤 다음 명령을 실행한다.

```powershell
gofmt -w cmd internal
$env:GOCACHE = "$PWD\.gocache"
$env:GOMODCACHE = "$PWD\.gomodcache"
go build ./...
go test ./...
go vet ./...
git diff --check
```

문서만 수정한 경우에도 `git diff --check`를 실행한다.

Codex 샌드박스에서 Go 텔레메트리나 기본 캐시 경로 권한 경고가 발생하면 프로젝트의
`.gocache`와 `.gomodcache`를 사용한다. 이 디렉터리는 커밋하지 않는다.

## 작업 완료 기준

- 요청 범위를 벗어난 리팩터링이 없다.
- 신규 동작에 대응하는 테스트가 있다.
- 빌드, 테스트, vet, diff check가 통과한다.
- 문서와 실제 명령이 일치한다.
- 새 환경변수는 `.env.example`에 설명돼 있다.
- 외부 데이터 변경에는 출처, 사용 조건, 장애 시 동작이 문서화돼 있다.
- Git 상태에서 의도하지 않은 파일이 추가되지 않았다.

## Git 규칙

- 커밋 메시지는 특별한 요청이 없으면 한국어로 작성한다.
- 한 커밋에는 하나의 논리적 변경만 담는다.
- 생성물, 캐시, 비밀정보를 stage하지 않는다.
- 사용자 요청 없이 기존 커밋을 재작성하거나 force push하지 않는다.
- push 전에는 현재 브랜치와 원격 추적 상태를 확인한다.
